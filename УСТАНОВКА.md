# Handy для русской речи — установка и настройка

Это форк [cjpais/Handy](https://github.com/cjpais/Handy) с адаптацией под русскую речь через Groq Whisper (`whisper-large-v3`) — качество распознавания заметно выше, чем у локальных моделей.

Оригинальный Handy — open-source приложение для голосового ввода на Mac/Windows/Linux. Ты нажимаешь горячую клавишу, говоришь, отпускаешь — распознанный текст вставляется в активное поле любого приложения.

> Сборку докрутил и поддерживает **Сергей Сипита** — [@SipitaSergey](https://t.me/SipitaSergey) в Telegram. Пиши если что-то не работает, есть вопросы или идеи.

## Что добавлено в этом форке

**Модель:**
- **Groq Whisper (`whisper-large-v3`) как отдельная модель** в списке моделей Handy — в оригинальном Handy Groq использовался только для LLM-обработки текста, здесь это полноценный движок распознавания. Модель живёт на серверах Groq, локально ничего не крутится.

**Борьба с обрезанием длинных записей:**
- **WAV-чанкинг 25 секунд** — длинные записи режутся на куски, каждый транскрибируется отдельно. Убирает обрезание конца длинных сообщений (у Whisper внутреннее 30-секундное окно, из-за которого стабильно терялся конец).
- **VAD-обрезка тишины в конце** — Silero VAD находит последний фрейм речи и обрезает всё что после (Whisper не «додумывает» на месте тишины).
- **Слияние огрызка** — если после чанкинга остаётся кусок < 5 сек, склеивается с предыдущим (иначе Whisper на нём галлюцинирует "Продолжение следует…").
- **ffmpeg не требуется** — резка через `hound` (чистый Rust).

**Борьба с галлюцинациями:**
- **Retry отдельного чанка** — если чанк вернул явную галлюцинацию (`"и так далее."`, `"и т.д."`, субтитровый мусор, слишком мало символов на секунду аудио) — перепрашивается один раз. Whisper недетерминирован, повтор обычно даёт реальный текст.
- **Фильтры финального текста** — режут типичные YouTube-эндинги («Субтитры создавал…», «Редактор субтитров…», лишние «Да.»/«Нет.» после вопроса).
- **Ужатый regex** для «субтитры» — раньше упоминание слова где угодно в речи могло стереть весь конец транскрипции. Теперь фильтр требует конкретный суффикс.
- **Без Whisper prompt** — раньше Handy передавал в Groq «подсказку» со списком домашних слов (Геткурс, VS Code и т.п.), из-за чего Whisper при неуверенности вставлял эти слова прямо в текст. Убрано.

**Постобработка текста:**
- **Автоматическое разбиение на абзацы** для длинных транскрипций — читаемость выросла.
- **Условный LLM-постпроцессинг** через Groq Llama — можно включить в настройках Handy для дополнительной чистки текста (пунктуация, орфография). В оригинале работал безусловно и портил результат мета-комментариями.

**Инфраструктурное:**
- Запросы к Groq идут через системный `curl` с `--noproxy '*'` — не зависят от системного прокси.

Полные технические детали каждого патча — в [OUR-PATCHES.md](OUR-PATCHES.md).

## Установка

### 1. Скачай сборку под свою систему

На странице [Releases](https://github.com/leonovs0808-star/Handy/releases) возьми файл под свою платформу:

- **Mac Apple Silicon** (M1/M2/M3/M4): `Handy_x.x.x_aarch64.dmg`
- **Mac Intel**: `Handy_x.x.x_x64.dmg`
- **Windows 10/11**: `Handy_x.x.x_x64-setup.exe`
- **Linux**: `.AppImage` или `.deb`

### 2. Установи

**Mac:**
1. Открой скачанный `.dmg`
2. Перетащи Handy в Applications
3. При первом запуске macOS скажет "неопознанный разработчик" — это нормально, приложение подписано ad-hoc
4. Правая кнопка мыши по Handy → **Open** → **Open Anyway**
5. Разреши доступ к микрофону и Accessibility, когда попросит

**Windows:**
1. Запусти скачанный `.exe`
2. SmartScreen скажет "Windows защитил ваш ПК" — это тоже нормально
3. Жми **More info** → **Run anyway**
4. Пройди установщик

### 3. Получи Groq API-ключ

Ключ бесплатный, есть щедрый бесплатный лимит.

1. Зайди на [console.groq.com](https://console.groq.com)
2. Зарегистрируйся (Google/email)
3. **API Keys** → **Create API Key** → скопируй строку `gsk_...`

### 4. Вставь ключ в Handy

1. Открой Handy
2. **Settings** → раздел **Post-Processing** или **API keys** (в зависимости от версии)
3. Поле **Groq API key** — вставь скопированный `gsk_...`
4. В **Models** выбери **Groq Whisper Large v3**
5. В **Language** выбери **Russian**

Готово.

## Как пользоваться

1. Кликни в любое текстовое поле (браузер, редактор, чат, что угодно)
2. Зажми `Option + Space` (Mac) или `Ctrl + Space` (Windows/Linux)
3. Говори
4. Отпусти — текст вставится сам

Есть альтернативный режим — двойное нажатие: одно включает запись, второе останавливает. Настраивается в Settings → General.

## Частые вопросы

**Groq стоит денег?**
Бесплатный лимит покрывает обычное использование одного человека. Если упрёшься — оплата минимальная, посмотри тарифы на console.groq.com.

**Аудио уходит в Groq — это безопасно?**
Groq — американская компания, аудио уходит на их серверы для распознавания. Если критична приватность — используй встроенные в Handy локальные модели (Whisper, Parakeet), только у них качество для русского хуже.

**Handy молчит / выдаёт ошибку "Groq API error"**
Проверь: 1) API-ключ вставлен и корректный, 2) есть интернет, 3) выбрана модель Groq, а не локальная.

**Обновления**
Когда выйдет новая версия — скачай новый файл со страницы Releases, установи поверх. Настройки сохранятся.

## Поддержка

Проблемы / предложения — заводи issue в этом репозитории.

Оригинальный Handy — с ним в Discord и на [handy.computer](https://handy.computer).
